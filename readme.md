# Macro Calculator (Streamlit)

A simple macro calculator using BMR (Katch–McArdle or Mifflin–St Jeor), activity multipliers, and flexible macro rules.

## Run locally
pip install -r requirements.txt
streamlit run app.py